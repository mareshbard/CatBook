<!DOCTYPE html>
<html lang="en">

<head>
  <link rel="stylesheet" href="stylesheets/style2.css">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CATBOOKS</title>
  <script src="https://kit.fontawesome.com/5dedad8172.js" crossorigin="anonymous"></script>
  <link href="https://fonts.googleapis.com/css2?family=Silkscreen:wght@400;700&display=swap" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:ital,wght@0,100..700;1,100..700&display=swap" rel="stylesheet">
  <script>
    function redirectToDetails(imageId) {
      window.location.href = "/book-details?image=" + imageId;
    }
  </script>
</head>

<body>

  <header>
    <div class="container">
      <div class="logo">
        <h1>CATBOOKS</h1>
      </div>
      <nav>
        <a href="/">Sair</a>
        <a href="/sobre">Sobre o projeto</a>
      </nav>
      <div class="sociais">
        <a href="https://www.instagram.com/let_gomesv">
          <button>
            <i class="fa fa-instagram"></i>
          </button>
        </a>
        <a href="https://github.com/mareshbard">
          <button>
            <i class="fa fa-github"></i>
          </button>
        </a>
      </div>
    </div>
  </header>

  <div class="container2">
    <img src="img/start.png" alt="banner">
    <div class="container3">
              <div class="container3">
                <div class="traço"></div>
                <h3>Lançamentos</h3>

                <ul>
                  <% if (livros && livros.length > 0) { %>
                    <% livros.filter(livro => livro.categoria === 'lancamento').forEach(function(livro) { %>
                      <li>
                        <a href="#" onclick="redirectToDetails(<%= livro.id %>)" rel="noopener noreferrer">
                          <button class="image-button">
                            <img src="<%= livro.imagem %>" alt="<%= livro.titulo %>">
                          </button>
                       
                        </a>
                      </li>
                    <% }); %>
                  <% } else { %>
                    <li>Não há livros de Lançamentos para exibir.</li>
                  <% } %>
                </ul>

                <div class="traço"></div>
                <h3>Clássicos</h3>

                <ul>
                  <% if (livros && livros.length > 0) { %>
                    <% livros.filter(livro => livro.categoria === 'classico').forEach(function(livro) { %>
                      <li>
                        <a href="#" onclick="redirectToDetails(<%= livro.id %>)" rel="noopener noreferrer">
                          <button class="image-button">
                            <img src="<%= livro.imagem %>" alt="<%= livro.titulo %>">
                          </button>
                        
                        </a>
                      </li>
                    <% }); %>
                  <% } else { %>
                    <li>Não há livros de Clássicos para exibir.</li>
                  <% } %>
                </ul>

                <div class="traço"></div>
              </div>
            </div>
</body>

</html>
